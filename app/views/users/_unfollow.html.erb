<%= form_for(current_user.active_relationships.find_by(followed_id: @users.id),
 html: { method: :delete },
 remote: true) do |f| %>
  <%= f.submit 'リムーブ', class: "btn-outline-dark btn mt-3" %>
<% end %>
