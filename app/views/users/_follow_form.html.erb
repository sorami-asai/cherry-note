<% unless current_user == @users %>
  <div id="follow_form">
  <% if current_user.following?(@users) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
